import styles from '../styles/MyCard.module.css';
import { useState } from 'react';
/* 
function MyCard({title, userName, password, shoe_desc, shoe_price, cart}){ 
    const [Name, setName]=useState(shoe_name);
    const[id,setid]=useState(shoe_id);
    const[Color, setColor]=useState(shoe_color);
    const[Description,setDescription]=useState(shoe_desc);
    const[Price, setPrice]=useState(shoe_price);
    const[Cart, setCart]=useState(cart);
    
    
    return(
        <div className = {styles.card_container}> 
            <h1> {shoe_name} </h1>
            <p> {shoe_id} </p>
            <p> {shoe_color} </p>
            <p> {shoe_desc} </p>
            <h2> {shoe_price}</h2>
            <p> {cart} </p>
            <p className = {styles.shoe_font}> {shoe_name} </p>
            <button> Add to cart </button> 
        </div>
    );
}
export default MyCard;



<div className={styles.card}>
                <form>
                    <div className={styles.Login}>
                        <h2> Log In to Your Account</h2>
                        <label className={styles.formLabel}>Username </label>
                        <input type="email" className={styles.formControl} id="loginEmail" />
                        <label className={styles.formLabel}>Password</label>
                        <input type="email" className={styles.formControl} id="loginPassword" />
                        <p> Forgot Password? </p>
                        <button type="submit" className={styles.btnPrimary}>Login</button>
                    </div>
                    <div className={styles.Account}>
                        <h2> Create An Account </h2>
                        <label className={styles.formLabel}>Username</label>
                        <input type="email" className={styles.formControl} id="accountEmail" />
                        <label className={styles.formLabel}>Password</label>
                        <input type="password" className={styles.formControl} id="accountPassword" />
                        <div className={styles.formCheck}>
                            <input className={styles.formCheckInput} type="radio" name="userType" id="userType1" value="reader" />
                            <label className={styles.formCheckLabel}>
                            Agree to Privacy Policy and Terms of Service*
                            </label>
                        </div>
                    </div>
                    <button type="submit" className={styles.btnPrimary}>Sign Up</button>
                    </form>
        </div>
        */